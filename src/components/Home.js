import React from "react";
import Header from "./Header";
import Main from "./Main";

const Home = (props) => {
  return (
    <div>
        <Header />
        <Main />
    </div>
  );
};

export default Home;
